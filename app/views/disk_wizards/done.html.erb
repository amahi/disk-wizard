<div class="row">
	<br />
	<% if user_selections['debug'] %>
	<div class="col-md-10 col-md-offset-1">
		<div class="panel panel-warning">
			<div class="panel-heading">
				Debug Information <span class="glyphicon glyphicon-info-sign"></span>
			</div>
			<div class="panel-body text-left">
				<p class="text-muted">
					Note:
				</p>
				<p class="text-muted">
					1. None of the commands got executed on HDA.
				</p>
				<p class="text-muted">
					2. Pipeline character(|) has encoded as exclamation character(!).
				</p>
				<p class="text-muted">
					3. Order(#) of the commands does matter if you try to execute them manually.
				</p>
			</div>
			<table class="table table-bordered table-hover">
				<thead>
					<tr class="active">
						<th class="text-center text-primary" style="width: 5%;">#</th>
						<th class="text-center text-primary" style="width: 20%;">Command name</th>
						<th class="text-center text-primary" >Parameters</th>
					</tr>
				</thead>
				<tbody>
					<%
					count = 0
					@operations.each do |operation|
					count += 1
					%>
					<% if operation[:name].eql? 'exception' %>
					<tr class="warning">
						<td style="vertical-align: middle;"><span class="glyphicon glyphicon-exclamation-sign"></span></td>
						<td><%= operation[:name] %></td>
						<td class="text-justify" ><% operation[:message].try(:each_line) do |line| %>
						<%= line %>
						<br />
						<% end %> </td>
					</tr>
					<% else %>

					<tr>
						<td style="vertical-align: middle;"><%= count %></td>
						<td><%= operation[:name] %></td>
						<td class="text-justify" ><% operation[:parameters].try(:each_line) do |line| %>
						<%= line %>
						<br />
						<% end %> </td>
					</tr>
					<% end %>
					<% end %>
				</tbody>
			</table>
		</div>
		<% else @user_selections["format"] %>
		<div class="col-md-6 col-md-offset-3">
			<table class="table">
				<thead>
					<tr>
						<th>Disk name</th>
						<th>New filesystem type</th>
						<th>Mount point</th>
					</tr>
				</thead>
				<tbody>
					<tr>
						<td><%= @user_selections["kname"] %></td>
						<td><%= @user_selections["fs_type"] %></td>
						<td>/var/hda/disks/<%= @user_selections["kname"] %></td>
					</tr>
				</tbody>
			</table>
			<% end %>
			<div class="col-md-6 col-md-offset-3 well well-sm">
				<a href="/tab/disks/disk_wizards" class="btn btn-success"> Continue with another device </a>
			</div>
		</div>
	</div>
